# Session Handoff — semantic-model

## Repository state
- Repository: `Mareta3rd/illo-and-killo-p-blico-`
- Active branch: `feature/semantic-model`
- Latest confirmed full suite in this session: **387 tests, OK**.
- The semantic-model work must continue from the current branch; do not assume `main` contains the latest work.

## Architecture already completed
The project has validated boundaries for:
- evidence evaluation and perceptual evidence
- candidate end-to-end orchestration
- semantic regression detection
- orchestrator semantic audit
- execution audit / immutable evidence fingerprints
- external evidence adapter + frozen snapshots
- simulated provider end-to-end path
- external evidence gateway
- provider registry
- real provider adapter/conformance contracts
- contract compatibility audit
- provider observation → snapshot → Core pipeline composition

The current evidence path is:

provider
→ ExternalEvidenceRecord
→ ProviderEvidenceObservation
→ EvidenceSnapshot
→ existing Core pipeline/evaluator
→ Core decision

## Gemini integration status
Gemini is the first real external provider and is now validated through the full evidence-to-Core path.

Implemented/validated:
- `core/gemini_evidence_adapter.py`
- `core/gemini_real_transport.py`
- `core/provider_evidence_observation.py`
- `scripts/run_gag001_gemini_experiment.py`
- Gemini unit/integration/conformance tests
- provider observation/snapshot/pipeline tests

Important compatibility details:
- `GeminiEvidenceAdapter.from_interactions_client(...)` exists and uses the concrete Interactions transport.
- `build_gemini_transport(...)` remains available because older adapter tests import it.
- The provider observation/snapshot composition is provider-neutral and must be reused by future providers.

## Real Gemini environment
In the Codespace:
- `google-genai` is installed (`2.19.0` in the validated environment).
- `from google import genai` works.
- `GEMINI_API_KEY` is configured as a GitHub Codespaces secret and reaches the environment.
- `genai.Client()` initializes successfully.
- A real connectivity call succeeded with model `gemini-3.6-flash` and returned `GEMINI_REAL_OK`.
- Do **not** put the API key in chat, source, commits, or documentation.

## Gemini end-to-end milestone
The final real run used:
- image: `gags/images/001_jamon.png`
- provider: `gemini`
- model: `gemini-3.6-flash`
- run id: `real-gag001-mosquito-20260826-01`
- invariant: `fauna/mosquito_tigre/readable_as_mosquito`

Real result:
- provider verdict/state: `CONFIRMED`
- `supporting_sources=["image"]`
- `canonical_evaluation`: `pass`
- final Core decision: `accept`
- `stop_reason`: `null`

Gemini returned evidence only. The `accept` decision was generated by the Core after the evidence crossed the snapshot boundary and the candidate contained the required explicit Core checks.

A separate real Gag 001 composition run validated that `gag/001/composition/illo_primary` is preserved literally as a canonical claim and does not receive an artificial three-segment contract evaluation.

An earlier real Gemini response returned `CONFIRMED` without supporting sources and was correctly rejected by the parser. The interaction contract was then strengthened so image-based confirmation explicitly uses `supporting_sources=["image"]`.

## Canon decision
Only **Gag 001 · Jamón** is currently accepted as canonical visual material.
- `gags/001_jamon.md` is the canonical specification.
- Gag 002 is **not canonical** and should not be promoted merely to enlarge the corpus.
- Distinguish `CANON` from any future synthetic/negative/ambiguous `TEST CORPUS`.

The canonical Gag 001 visual hierarchy remains **Illo → jamón → reaction of Killo**.

## Current image
The working canonical image used by the real experiment is:
`gags/images/001_jamon.png`

Do not promote additional images to canon without human review.

## Custom Copilot agent
Repository custom agent:
`.github/agents/semantic-boundary-engineer.agent.md`

Repository-wide guardrails:
`.github/copilot-instructions.md`

The custom agent is the local execution/development counterpart to the architectural reasoning in the main project conversation. It has successfully performed inspection, documentation maintenance, controlled implementation, focused tests, full-suite tests, and scoped commits while preserving the Core boundary.

## Next phase — OpenAI
The next session should begin the second-provider conformance phase.

Target architecture:

OpenAI transport
→ OpenAI provider adapter
→ ExternalEvidenceRecord
→ ProviderEvidenceObservation
→ EvidenceSnapshot
→ existing Core pipeline/evaluator

Do not create a parallel OpenAI-specific Core path.

Planned sequence:
1. Verify current OpenAI SDK/API/model availability in the live environment.
2. Implement provider transport/parser with injected/fake transport.
3. Add adapter conformance tests.
4. Reuse the provider-neutral observation/snapshot/pipeline composition.
5. Validate real authentication/connectivity using an environment secret.
6. Run one real perceptual invariant experiment.
7. Run one real end-to-end Core decision experiment.
8. Full-suite regression after each meaningful block.

OpenAI SDK methods, authentication, endpoints, model identifiers, and response schemas must be verified live when this phase starts; do not assume them from memory.

## Working principles
- Do not invent test results, file paths, API behavior, or repository state.
- When a test fails, fix it before advancing.
- After a new block, run its focused tests and then the complete suite.
- Prefer architectural cleanliness and explicit contracts over rushing toward more features.
- Keep real provider credentials outside the repository.
- Preserve `UNKNOWN`; never manufacture certainty.
- Never let a provider output a Core decision directly.

## Continuity
The authoritative durable handoff is `docs/AI_HANDOFF.md`. On a new session, read it first, confirm the branch and full-suite checkpoint, then continue with OpenAI conformance work.
