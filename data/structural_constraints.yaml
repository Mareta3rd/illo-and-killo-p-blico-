version: "0.1"

# Canonical structural constraints for deterministic invariants.
# required_paths assert presence; expected asserts exact values at paths.

structural:
  characters:
    xoxo:
      white_pelage:
        expected:
          - path: [body, color]
            value: white
      yellow_blonde_flame_tuft:
        expected:
          - path: [mane_tuft, color]
            value: yellow_blonde
          - path: [mane_tuft, shape]
            value: flame_like_crest
      green_scarf:
        expected:
          - path: [scarf, color]
            value: green
      black_hands:
        expected:
          - path: [legs, hands, color]
            value: black
      three_finger_hands:
        expected:
          - path: [legs, hands, fingers]
            value: 3
      black_hooves:
        expected:
          - path: [legs, feet, type]
            value: hoof
          - path: [legs, feet, color]
            value: black
      short_flame_tail:
        expected:
          - path: [tail, type]
            value: flame
          - path: [tail, length]
            value: short
      stylized_equine_hint:
        expected:
          - path: [muzzle]
            value: equine_hint_integrated
    pisha:
      red_pelage:
        expected:
          - path: [body, color]
            value: red
      black_spots:
        expected:
          - path: [spots, color]
            value: black
      clavel:
        expected:
          - path: [flower, type]
            value: clavel
      compact_round_shape:
        expected:
          - path: [body, shape]
            value: compact_round
      short_rounded_horns:
        expected:
          - path: [horns, type]
            value: short_rounded
      black_hands:
        expected:
          - path: [legs, hands, color]
            value: black
      three_finger_hands:
        expected:
          - path: [legs, hands, fingers]
            value: 3
      black_hooves:
        expected:
          - path: [legs, feet, type]
            value: hoof
          - path: [legs, feet, color]
            value: black
      bovine_hint:
        expected:
          - path: [muzzle]
            value: simple_bovine_hint

status:
  policy: "Missing structural constraints remain unknown and require explicit human resolution; they are never inferred."
